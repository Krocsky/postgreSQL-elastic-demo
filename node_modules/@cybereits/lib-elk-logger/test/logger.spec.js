const { createLogger, LoggerType } = require('../src/index')

const logicLogger = createLogger('lib-elk-logger', {
  loggerType: LoggerType.BUSINESSLOG,
  tags: ['lib-logicLogger-test'],
  isDev: true,
})

const sysLogger = createLogger('lib-elk-logger', {
  loggerType: LoggerType.SYSTEMLOG,
  tags: ['lib-sysLogger-test'],
})

logicLogger.info('this is a test information of lib-elk-logger.【' + Math.random() + '】')
sysLogger.info('this is a test information of lib-elk-logger.【' + Math.random() + '】')
